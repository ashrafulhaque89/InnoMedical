<%= form_with(model: doctor_app, local: true) do |form| %>
  <% if doctor_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doctor_app.errors.count, "error") %> prohibited this doctor_app from being saved:</h2>

      <ul>
      <% doctor_app.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :doctor_id %>
    <%= form.collection_select :doctor_id, Doctor.all, :id, :field %>
  </div>

  <div class="field">
    <%= form.label :appointment_id %>
    <%= form.collection_select :appointment_id, Appointment.all, :id, :startdate  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
